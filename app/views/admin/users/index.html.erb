<%= link_to "Add User", new_admin_user_path %>

<% @users.each do |user| %>
  <p><%= user.full_name %> (<%= link_to "edit", edit_admin_user_path(user) %>) (<%= link_to "delete", admin_user_path(user), method: :delete, confirm: "You sure?" %>)</p>
<% end %>

<%= paginate @users %>